# 更新记录

## 1.4.0 - 首个正式发行版

- 正式发布前将项目由 PerformanceTable 更名为 MacVitals，并统一 Python 包、模块入口和 CLI 命令为 `macvitals`；旧名称未形成公开兼容承诺。
- 增加可选 `--dev` 模式：在实时界面显示当前指标来源、语义、状态和数据年龄。
- 增加独立 `--ioreport-probe` 只读探针，验证 CPU performance states、Energy Model 和 DCP swap 的两次采样差分。
- 增加管理员专用 `--compare-probes`，在同一短窗口并行对照 `powermetrics` 与 IOReport。
- 诊断 JSON 增加 `source_meta`，只描述当前快照，不保存历史记录或日志。
- 明确 Stats、活动监视器与 MacVitals 的同名指标不保证同一统计口径。
- 正式界面默认不显示开发元数据，SSH 随用随开定位不变。
- 增加受控负载验证：允许约 10% 的非零基线，检查单核刻度、多核上升与负载结束后的恢复。
- 完成 powermetrics 中止降级验证：特权字段清空并标记不可用，基础 CPU、进程和传感器继续工作。
- 44 项自动测试、真实伪终端缩放和 M5 Pro 现场验证通过。
- 进程页按当前 CPU/RSS 排序后只展示前 15 个进程，采集层仍保留完整快照用于差分。
- README 明确 SSH 轻量监控定位、与 Stats 等 GUI 工具的边界、社区兼容贡献流程；正式发行采用 MacVitals Non-Commercial Share-Alike License。

## 1.3.2 — 2026-09-21

- 修复管理员诊断固定等待时间过短的问题：等待真实完整采样，失败时有超时上限。
- 诊断补充 ANE 功率，缺失时同样输出 null。
- M5 Pro 实机同帧核对 P0/P1/S 频率、GPU 频率和活跃率、CPU/GPU/ANE 功率与热压力；验证 0.5/1 秒连续采样及子进程清理。
- 单核受控负载实测约 5.56% 整机占用，符合 18 核归一化口径。保留原有彩色分块界面。
- 33 项自动测试通过；新增可重现的特权实测脚本与脱敏记录。

## 1.3.1 — 2026-09-21

- 恢复原有分块仪表盘设计语言：模块彩色标题、分隔线、使用率条、温度强调与趋势图。
- 总览宽屏双栏，CPU/GPU、内存/网络、磁盘/系统成组排列；窄屏按完整模块堆叠。
- 详情页按指标/趋势/说明分区，传感器按 CPU、GPU、机身和散热分区。
- 保留 1.3.0 的采集管线、未知值语义、中文宽度保护与全页滚动。
- 33 项测试通过，新增模块颜色与跨列边界回归，正常终端总览无需滚动。

## 1.3.0 — 2026-09-21

### 数据与兼容性

- 从 sysctl 名称和数量读取核心拓扑，支持 M5 Pro 的超级核心 + 性能核心组合。
- 加入 M5 Pro/Max 已知温度映射。测点编号与逻辑核编号分开，不再按核心数截断探头列表。
- 总 CPU 用 Mach 累计 ticks 差分；进程 CPU 用累计 CPU 秒差分，PID 与启动时间共同识别进程。首帧和新进程显示未知。
- 修复 File-backed pages 的连字符解析；内存压力读取内核等级，不再用命令退出码推断。
- 磁盘查询限定 IOBlockStorageDriver 自身深度，避免下层分区/APFS 重复计数；设备变化或计数回退重建基线。
- 启动磁盘空间使用 APFS 共享空间口径，避免只显示系统只读卷。
- 修复无硬件地址的 VPN 接口行解析；路由切换时重建速率基线。
- 速率分母使用计数采样时的单调时钟，避免系统校时和前置命令延迟造成异常。

### 管线与界面

- powermetrics 先探测采样器，按完整帧提交；保留系统原始集群频率身份，不猜测核编号归属。
- GPU 活跃驻留率与活跃频率分开。缺失/过期字段显示 `--`，不伪装为 0 W 或空闲。
- 清理失效温度，使用完整快照隔离界面与采集线程，退出前协调线程和硬件连接清理。
- 六页均可滚动；正常大小终端总览保持同屏；支持中文显示宽度、八色和单色终端。
- 明确 RSS、匿名内存估算、GiB/MiB、测点平均温度与非整机功率的口径。

### 工程与验证

- 增加 pyproject、`macvitals` 安装入口、`python3 -m macvitals` 入口。
- 新增 `--diagnose` JSON 诊断，不可用数值为 null，省略进程名称/命令行。
- 32 项测试通过，含真实 macOS 伪终端多尺寸和三种 TERM 验证。
- 普通权限下在 M5 Pro / macOS 26.6.2 实测；特权 powermetrics 实机核对尚未完成。
- 项目说明与作品页文案同步到用户 Obsidian 的 MacVitals 目录。

### 边界

SMC 键的中文物理名称参考社区映射，不是 Apple 官方契约。M5 Pro 当前没有已确认的内存温度映射，暂不显示此类测点；不能推断该机器没有内存温度传感器。新增核心/传感器支持不等于全系列机型均已验证。公开仓库地址和发行签名仍待配置。
